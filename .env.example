# Set this after running `cd contracts && npm run deploy:local`.
GOLDEN_LOCK_ADDRESS=0x0000000000000000000000000000000000000000
RPC_URL=https://your-managed-rpc.example
NEXT_PUBLIC_RELAY_URL=https://relay.example.com

# Local Hardhat account #0. Development only.
RELAYER_PRIVATE_KEY=0xac0974bec39a17e36ba4a6b4d238ff944bacb478cbed5efcae784d7bf4f2ff80

# Relay Socket.IO access configuration. Use a different random value outside local development.
ALLOWED_ORIGINS=http://localhost:3000
ALLOW_DEMO_PROOF=false
ALLOW_RULE_BASED_FALLBACK=false
POSTGRES_DB=goldenlock
POSTGRES_USER=goldenlock
POSTGRES_PASSWORD=replace-with-a-secret
REDIS_PASSWORD=replace-with-a-secret
AUTH_JWT_SECRET=replace-with-a-random-secret-at-least-32-characters